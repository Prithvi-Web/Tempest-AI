{"default":{"identifier":"default","description":"Main window: core defaults only — the sidecar is spawned and owned by the Rust host itself (no shell plugin, no webview-reachable process execution).","local":true,"windows":["main"],"permissions":["core:default"]}}