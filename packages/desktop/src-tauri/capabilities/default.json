{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core defaults only — the sidecar is spawned and owned by the Rust host itself (no shell plugin, no webview-reachable process execution).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
