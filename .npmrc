# C3: upstream's `"*"` cross-deps (@librechat/client, librechat-data-provider) must
# resolve to the VENDORED workspace packages, never the registry copies of those names.
link-workspace-packages = true
